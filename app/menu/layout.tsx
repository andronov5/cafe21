import type { Metadata } from "next";
export const metadata: Metadata = { title: "The menu | Café 21 Express" };
export default function PageLayout({ children }: { children: React.ReactNode }) { return children; }
